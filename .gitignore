.DS_Store
**/.DS_Store

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env/

vectorstore/

scr/

__pycache__/
vectorstore/

# Secrets. main's .gitignore only had `.env/` (a directory), which does not
# match the `.env` FILE holding HIMEDIA_API_KEY and GEMINI_API_KEY.
.env

# Conversation trace - regenerated on every run, and it contains real
# task titles and staff names. Deleted from the repo on main; keep it out.
audit.log
