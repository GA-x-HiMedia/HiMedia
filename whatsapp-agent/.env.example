# Copy to .env — never commit the real .env (first line of .gitignore).
# All values intentionally blank here, including HIMEDIA_API_KEY — copy
# the real shared class key from the handbook into your own .env, not
# into this file.

# --- HiMedia sandbox ---
HIMEDIA_BASE_URL=
HIMEDIA_API_KEY=

# --- OpenAI (given to you separately, per the handbook) ---
OPENAI_API_KEY=

# --- WhatsApp Cloud API (Chapter 28 — from your own free Meta developer app) ---
WHATSAPP_TOKEN=
WHATSAPP_PHONE_ID=
WHATSAPP_VERIFY_TOKEN=
