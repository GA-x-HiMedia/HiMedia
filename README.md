# HiMedia WhatsApp Agent

**General Assembly × JoinFuture Solutions W.L.L. — Two-Week Capstone**
Students: Sara Alnajjar, Reem AlShehabi, Zainab Mohammed.

## How to run it

```bash
python3.11 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
# then edit .env: set a real OPENAI_API_KEY (HIMEDIA_API_KEY is already
# filled in with the shared class sandbox key from the handbook)
